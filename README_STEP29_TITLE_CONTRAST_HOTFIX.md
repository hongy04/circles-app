# Step 29 Timeline Event Title Contrast Hotfix

Apply on top of Step 29 + the prior event-title fallback hotfix.

## Fix
- Event memory summary titles now sit on a dark translucent glass plate, so white text remains readable on pale preset Event Looks as well as bright custom covers.
- The memory date gets its own dark glass chip for the same reason.
- The carousel, title fallback logic, inline photos, All photos, and Event details behavior are unchanged.

No migration, Edge Function, native code, or EAS build is required.
