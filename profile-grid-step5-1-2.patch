--- /mnt/data/circles_profile_fix_step5_1/src/components/profile/ProfilePostGridItem.js	2026-07-20 20:00:09.804923401 +0000
+++ /mnt/data/circles_profile_grid_fix_step5_1_2/src/components/profile/ProfilePostGridItem.js	2026-07-20 20:56:17.856737527 +0000
@@ -1,31 +1,43 @@
-import React from 'react';
+import React, { useEffect, useState } from 'react';
 import { Image, Pressable, StyleSheet, Text, View } from 'react-native';
 import Ionicons from '@expo/vector-icons/Ionicons';
 import { COLORS } from '../../theme/colors';
 
-export function ProfilePostGridItem({ post, onPress }) {
+export function ProfilePostGridItem({ post, onPress, size }) {
+  const [imageFailed, setImageFailed] = useState(false);
   const isVideo = post.mediaType === 'video';
+  const canShowImage = Boolean(post.previewUrl) && !isVideo && !imageFailed;
+
+  useEffect(() => {
+    setImageFailed(false);
+  }, [post.previewUrl]);
 
   return (
     <Pressable
       onPress={onPress}
       style={({ pressed }) => [
         styles.root,
+        size ? { width: size, height: size } : styles.fallbackSize,
         pressed && styles.pressed,
       ]}
     >
-      {post.previewUrl && !isVideo ? (
+      <View style={styles.mediaFallback}>
+        <Ionicons
+          name={isVideo ? 'play' : 'image-outline'}
+          size={isVideo ? 30 : 27}
+          color="#fff"
+        />
+        <Text style={styles.fallbackLabel}>{isVideo ? 'Video' : 'Photo'}</Text>
+      </View>
+
+      {canShowImage ? (
         <Image
           source={{ uri: post.previewUrl }}
           style={StyleSheet.absoluteFillObject}
           resizeMode="cover"
+          onError={() => setImageFailed(true)}
         />
-      ) : (
-        <View style={styles.videoFallback}>
-          <Ionicons name="play" size={30} color="#fff" />
-          <Text style={styles.videoLabel}>Video</Text>
-        </View>
-      )}
+      ) : null}
 
       {isVideo ? (
         <View style={styles.videoBadge}>
@@ -45,24 +57,27 @@
 
 const styles = StyleSheet.create({
   root: {
-    flex: 1 / 3,
-    maxWidth: '33.333333%',
-    aspectRatio: 1,
+    flexGrow: 0,
+    flexShrink: 0,
     backgroundColor: '#efefef',
     borderWidth: StyleSheet.hairlineWidth,
     borderColor: COLORS.bg,
     overflow: 'hidden',
   },
+  fallbackSize: {
+    width: '33.333333%',
+    aspectRatio: 1,
+  },
   pressed: {
     opacity: 0.82,
   },
-  videoFallback: {
+  mediaFallback: {
     ...StyleSheet.absoluteFillObject,
-    backgroundColor: '#222',
+    backgroundColor: '#353535',
     alignItems: 'center',
     justifyContent: 'center',
   },
-  videoLabel: {
+  fallbackLabel: {
     color: '#fff',
     fontFamily: 'Manrope_600SemiBold',
     fontSize: 11,
--- /mnt/data/circles_profile_fix_step5_1/src/screens/profile/ProfileViewScreen.js	2026-07-20 20:08:08.058481774 +0000
+++ /mnt/data/circles_profile_grid_fix_step5_1_2/src/screens/profile/ProfileViewScreen.js	2026-07-20 20:56:18.890107016 +0000
@@ -114,6 +114,7 @@
   const [refreshing, setRefreshing] = useState(false);
   const [error, setError] = useState('');
   const [actionBusy, setActionBusy] = useState(false);
+  const [gridWidth, setGridWidth] = useState(0);
 
   const load = useCallback(async ({ refresh = false } = {}) => {
     if (refresh) setRefreshing(true);
@@ -242,7 +243,15 @@
 
   return (
     <SafeAreaView edges={['top']} style={styles.screen}>
-      <View style={styles.contentWidth}>
+      <View
+        style={styles.contentWidth}
+        onLayout={(event) => {
+          const nextWidth = Math.floor(event.nativeEvent.layout.width);
+          setGridWidth((currentWidth) =>
+            currentWidth === nextWidth ? currentWidth : nextWidth
+          );
+        }}
+      >
         <FlatList
           data={posts}
           keyExtractor={(item) => item.id}
@@ -251,6 +260,7 @@
           renderItem={({ item }) => (
             <ProfilePostGridItem
               post={item}
+              size={gridWidth > 0 ? Math.floor(gridWidth / 3) : undefined}
               onPress={() => navigation.navigate('PostDetail', { postId: item.id })}
             />
           )}
@@ -269,6 +279,7 @@
             />
           )}
           contentContainerStyle={styles.listContent}
+          columnWrapperStyle={styles.gridRow}
           showsVerticalScrollIndicator={false}
         />
       </View>
@@ -294,6 +305,9 @@
     flexGrow: 1,
     paddingBottom: 42,
   },
+  gridRow: {
+    alignItems: 'flex-start',
+  },
   topBar: {
     minHeight: 52,
     flexDirection: 'row',
