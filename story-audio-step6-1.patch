diff -ruN /mnt/data/circles_story_audio_step6_1_work/circles_stories_polish_step6_package/README_STORY_AUDIO_FIX_STEP_6_1.md /mnt/data/circles_story_audio_step6_1_package/README_STORY_AUDIO_FIX_STEP_6_1.md
--- /mnt/data/circles_story_audio_step6_1_work/circles_stories_polish_step6_package/README_STORY_AUDIO_FIX_STEP_6_1.md	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/circles_story_audio_step6_1_package/README_STORY_AUDIO_FIX_STEP_6_1.md	2026-07-20 21:28:14.090763460 +0000
@@ -0,0 +1,47 @@
+# Circles Step 6.1 — iPhone Story Audio Fix
+
+This patch fixes story videos that stayed silent on iPhone even when the story mute button showed the unmuted state.
+
+## Cause
+
+The story viewer correctly passed `isMuted={false}` to the video, but iOS still respects the phone's Ring/Silent switch unless the app audio session explicitly enables playback in silent mode.
+
+## Fix
+
+`StoryViewer.js` now configures the Expo AV audio session when the viewer mounts:
+
+- `playsInSilentModeIOS: true`
+- playback routed through the speaker rather than the Android earpiece
+- normal ducking behavior when another app owns audio
+- no background audio session
+
+The story video also explicitly uses full playback volume when unmuted.
+
+## Apply
+
+Copy this package into the project root and allow Windows to replace or merge files. Only `src/components/stories/StoryViewer.js` changed; `App.js` is included as the complete current checkpoint.
+
+No Supabase migration is required.
+
+## Test on iPhone 14
+
+```powershell
+cd C:\Users\honge\Dev\circles-app
+npx expo start --tunnel --clear
+```
+
+1. Keep the iPhone Ring/Silent switch in silent mode.
+2. Open a video story.
+3. Confirm audio plays when the volume-high icon is shown.
+4. Tap the icon and confirm audio stops.
+5. Tap it again and confirm audio returns.
+6. Raise the phone's media volume if the video is still quiet.
+
+## Commit
+
+```powershell
+git status
+git add App.js src README_STORY_AUDIO_FIX_STEP_6_1.md
+git commit -m "Fix iPhone story video audio"
+git push
+```
diff -ruN /mnt/data/circles_story_audio_step6_1_work/circles_stories_polish_step6_package/src/components/stories/StoryViewer.js /mnt/data/circles_story_audio_step6_1_package/src/components/stories/StoryViewer.js
--- /mnt/data/circles_story_audio_step6_1_work/circles_stories_polish_step6_package/src/components/stories/StoryViewer.js	2026-07-20 21:11:33.000000000 +0000
+++ /mnt/data/circles_story_audio_step6_1_package/src/components/stories/StoryViewer.js	2026-07-20 21:28:13.964355449 +0000
@@ -10,7 +10,7 @@
   View,
 } from 'react-native';
 import Ionicons from '@expo/vector-icons/Ionicons';
-import { Video } from 'expo-av';
+import { Audio, Video } from 'expo-av';
 import { getInitials } from '../../utils/getInitials';
 
 const IMAGE_DURATION_MS = 5000;
@@ -74,6 +74,18 @@
   }, []);
 
   useEffect(() => {
+    Audio.setAudioModeAsync({
+      allowsRecordingIOS: false,
+      playsInSilentModeIOS: true,
+      staysActiveInBackground: false,
+      shouldDuckAndroid: true,
+      playThroughEarpieceAndroid: false,
+    }).catch((error) => {
+      console.warn('Could not enable story audio playback', error);
+    });
+  }, []);
+
+  useEffect(() => {
     elapsedImageMsRef.current = 0;
     finishHandledRef.current = false;
     setProgress(0);
@@ -277,6 +289,7 @@
             resizeMode="contain"
             shouldPlay={!paused}
             isMuted={muted}
+            volume={1}
             onPlaybackStatusUpdate={(status) => {
               if (!status) return;
 
diff -ruN /mnt/data/circles_story_audio_step6_1_work/circles_stories_polish_step6_package/story-audio-step6-1.patch /mnt/data/circles_story_audio_step6_1_package/story-audio-step6-1.patch
--- /mnt/data/circles_story_audio_step6_1_work/circles_stories_polish_step6_package/story-audio-step6-1.patch	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/circles_story_audio_step6_1_package/story-audio-step6-1.patch	2026-07-20 21:28:14.105853310 +0000
@@ -0,0 +1,90 @@
+diff -ruN /mnt/data/circles_story_audio_step6_1_work/circles_stories_polish_step6_package/README_STORY_AUDIO_FIX_STEP_6_1.md /mnt/data/circles_story_audio_step6_1_package/README_STORY_AUDIO_FIX_STEP_6_1.md
+--- /mnt/data/circles_story_audio_step6_1_work/circles_stories_polish_step6_package/README_STORY_AUDIO_FIX_STEP_6_1.md	1970-01-01 00:00:00.000000000 +0000
++++ /mnt/data/circles_story_audio_step6_1_package/README_STORY_AUDIO_FIX_STEP_6_1.md	2026-07-20 21:28:14.090763460 +0000
+@@ -0,0 +1,47 @@
++# Circles Step 6.1 — iPhone Story Audio Fix
++
++This patch fixes story videos that stayed silent on iPhone even when the story mute button showed the unmuted state.
++
++## Cause
++
++The story viewer correctly passed `isMuted={false}` to the video, but iOS still respects the phone's Ring/Silent switch unless the app audio session explicitly enables playback in silent mode.
++
++## Fix
++
++`StoryViewer.js` now configures the Expo AV audio session when the viewer mounts:
++
++- `playsInSilentModeIOS: true`
++- playback routed through the speaker rather than the Android earpiece
++- normal ducking behavior when another app owns audio
++- no background audio session
++
++The story video also explicitly uses full playback volume when unmuted.
++
++## Apply
++
++Copy this package into the project root and allow Windows to replace or merge files. Only `src/components/stories/StoryViewer.js` changed; `App.js` is included as the complete current checkpoint.
++
++No Supabase migration is required.
++
++## Test on iPhone 14
++
++```powershell
++cd C:\Users\honge\Dev\circles-app
++npx expo start --tunnel --clear
++```
++
++1. Keep the iPhone Ring/Silent switch in silent mode.
++2. Open a video story.
++3. Confirm audio plays when the volume-high icon is shown.
++4. Tap the icon and confirm audio stops.
++5. Tap it again and confirm audio returns.
++6. Raise the phone's media volume if the video is still quiet.
++
++## Commit
++
++```powershell
++git status
++git add App.js src README_STORY_AUDIO_FIX_STEP_6_1.md
++git commit -m "Fix iPhone story video audio"
++git push
++```
+diff -ruN /mnt/data/circles_story_audio_step6_1_work/circles_stories_polish_step6_package/src/components/stories/StoryViewer.js /mnt/data/circles_story_audio_step6_1_package/src/components/stories/StoryViewer.js
+--- /mnt/data/circles_story_audio_step6_1_work/circles_stories_polish_step6_package/src/components/stories/StoryViewer.js	2026-07-20 21:11:33.000000000 +0000
++++ /mnt/data/circles_story_audio_step6_1_package/src/components/stories/StoryViewer.js	2026-07-20 21:28:13.964355449 +0000
+@@ -10,7 +10,7 @@
+   View,
+ } from 'react-native';
+ import Ionicons from '@expo/vector-icons/Ionicons';
+-import { Video } from 'expo-av';
++import { Audio, Video } from 'expo-av';
+ import { getInitials } from '../../utils/getInitials';
+ 
+ const IMAGE_DURATION_MS = 5000;
+@@ -74,6 +74,18 @@
+   }, []);
+ 
+   useEffect(() => {
++    Audio.setAudioModeAsync({
++      allowsRecordingIOS: false,
++      playsInSilentModeIOS: true,
++      staysActiveInBackground: false,
++      shouldDuckAndroid: true,
++      playThroughEarpieceAndroid: false,
++    }).catch((error) => {
++      console.warn('Could not enable story audio playback', error);
++    });
++  }, []);
++
++  useEffect(() => {
+     elapsedImageMsRef.current = 0;
+     finishHandledRef.current = false;
+     setProgress(0);
+@@ -277,6 +289,7 @@
+             resizeMode="contain"
+             shouldPlay={!paused}
+             isMuted={muted}
++            volume={1}
+             onPlaybackStatusUpdate={(status) => {
+               if (!status) return;
+ 
