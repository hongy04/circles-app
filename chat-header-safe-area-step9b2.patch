--- /mnt/data/circles_step9b1_work/App.js	2026-07-21 19:29:10.907730243 +0000
+++ /mnt/data/circles_step9b2_work/App.js	2026-07-21 20:05:27.440850661 +0000
@@ -232,7 +232,7 @@
       <CirclesStackNav.Screen
         name="Chat"
         component={ChatScreen}
-        options={({ route }) => ({ title: route.params?.name || 'Chat' })}
+        options={{ headerShown: false }}
       />
       <CirclesStackNav.Screen
         name="CreateGroup"
--- /mnt/data/circles_step9b1_work/src/screens/conversations/ChatScreen.js	2026-07-21 19:29:41.088475373 +0000
+++ /mnt/data/circles_step9b2_work/src/screens/conversations/ChatScreen.js	2026-07-21 20:05:27.441581885 +0000
@@ -13,7 +13,7 @@
   TextInput,
   View,
 } from 'react-native';
-import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useSafeAreaInsets } from 'react-native-safe-area-context';
 import { useFocusEffect } from '@react-navigation/native';
 import Ionicons from '@expo/vector-icons/Ionicons';
 import * as Haptics from 'expo-haptics';
@@ -194,31 +194,6 @@
     }
   }, [conversationId]);
 
-  useEffect(() => {
-    navigation.setOptions({
-      title: '',
-      headerTitleAlign: 'center',
-      headerStyle: { height: 70, backgroundColor: COLORS.bg },
-      headerRight: () => null,
-      headerTitle: () => (
-        <ConversationHeaderTitle
-          name={conversation?.title || name}
-          avatarUri={conversation?.avatar_url || initialAvatarUri}
-          onPress={() => navigation.navigate('CircleProfile', {
-            conversationId,
-          })}
-        />
-      ),
-    });
-  }, [
-    conversation?.avatar_url,
-    conversation?.title,
-    conversationId,
-    initialAvatarUri,
-    name,
-    navigation,
-  ]);
-
   useFocusEffect(
     useCallback(() => {
       load();
@@ -474,130 +449,162 @@
   }
 
   return (
-    <SafeAreaView edges={['bottom']} style={styles.screen}>
-      {loading ? (
-        <View style={styles.centerState}>
-          <ActivityIndicator />
-          <Text style={styles.stateText}>Opening private conversation…</Text>
-        </View>
-      ) : error ? (
-        <View style={styles.centerState}>
-          <Ionicons name="lock-closed-outline" size={34} color={COLORS.text} />
-          <Text style={styles.errorText}>{error}</Text>
-          <Pressable onPress={() => load()} style={styles.retryButton}>
-            <Text style={styles.retryText}>Try again</Text>
+    <View style={styles.screen}>
+      <View style={[styles.chatHeader, { paddingTop: insets.top }]}>
+        <View style={styles.chatHeaderRow}>
+          <Pressable
+            onPress={() => navigation.goBack()}
+            accessibilityRole="button"
+            accessibilityLabel="Back to Circles"
+            hitSlop={10}
+            style={({ pressed }) => [
+              styles.headerSide,
+              styles.backButton,
+              pressed && styles.pressed,
+            ]}
+          >
+            <Ionicons name="chevron-back" size={29} color={COLORS.text} />
           </Pressable>
+
+          <View style={styles.chatHeaderIdentity}>
+            <ConversationHeaderTitle
+              name={conversation?.title || name}
+              avatarUri={conversation?.avatar_url || initialAvatarUri}
+              onPress={() => navigation.navigate('CircleProfile', {
+                conversationId,
+              })}
+            />
+          </View>
+
+          <View style={styles.headerSide} />
         </View>
-      ) : messages.length === 0 ? (
-        <View style={styles.emptyState}>
-          <ConversationHeaderTitle
-            name={conversation?.title || name}
-            avatarUri={conversation?.avatar_url || initialAvatarUri}
-            onPress={() => navigation.navigate('CircleProfile', {
-              conversationId,
-            })}
-          />
-          <Text style={styles.emptyTitle}>A private Circle</Text>
-          <Text style={styles.emptyBody}>
-            Messages and shared media are visible only to accepted members.
-            Photos and videos you send will automatically become this Circle’s
-            Timeline.
-          </Text>
-        </View>
-      ) : (
-        <FlatList
-          ref={listRef}
-          data={messages}
-          keyExtractor={(item) => item.id}
-          renderItem={renderMessage}
-          inverted
-          keyboardShouldPersistTaps="handled"
-          contentContainerStyle={styles.messageList}
-        />
-      )}
-
-      {!loading && !error ? (
-        <KeyboardAvoidingView
-          behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
-          keyboardVerticalOffset={Platform.OS === 'ios' ? 92 : 0}
-        >
-          <SelectedMedia
-            assets={selectedAssets}
-            disabled={sending}
-            onRemove={(assetId) => setSelectedAssets((current) =>
-              current.filter((asset) => asset.id !== assetId)
-            )}
-          />
+      </View>
 
-          {uploadStage ? (
-            <View style={styles.uploadStage}>
-              <ActivityIndicator size="small" />
-              <Text style={styles.uploadStageText}>{uploadStage}</Text>
-            </View>
-          ) : null}
+      <View style={styles.chatBody}>
+        {loading ? (
+          <View style={styles.centerState}>
+            <ActivityIndicator />
+            <Text style={styles.stateText}>Opening private conversation…</Text>
+          </View>
+        ) : error ? (
+          <View style={styles.centerState}>
+            <Ionicons name="lock-closed-outline" size={34} color={COLORS.text} />
+            <Text style={styles.errorText}>{error}</Text>
+            <Pressable onPress={() => load()} style={styles.retryButton}>
+              <Text style={styles.retryText}>Try again</Text>
+            </Pressable>
+          </View>
+        ) : messages.length === 0 ? (
+          <View style={styles.emptyState}>
+            <ConversationHeaderTitle
+              name={conversation?.title || name}
+              avatarUri={conversation?.avatar_url || initialAvatarUri}
+              onPress={() => navigation.navigate('CircleProfile', {
+                conversationId,
+              })}
+            />
+            <Text style={styles.emptyTitle}>A private Circle</Text>
+            <Text style={styles.emptyBody}>
+              Messages and shared media are visible only to accepted members.
+              Photos and videos you send will automatically become this Circle’s
+              Timeline.
+            </Text>
+          </View>
+        ) : (
+          <FlatList
+            ref={listRef}
+            data={messages}
+            keyExtractor={(item) => item.id}
+            renderItem={renderMessage}
+            inverted
+            keyboardShouldPersistTaps="handled"
+            contentContainerStyle={styles.messageList}
+          />
+        )}
 
-          <View style={[
-            styles.composer,
-            { paddingBottom: Math.max(8, insets.bottom) },
-          ]}>
-            <Pressable
-              onPress={pickMedia}
+        {!loading && !error ? (
+          <KeyboardAvoidingView
+            behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
+            keyboardVerticalOffset={0}
+          >
+            <SelectedMedia
+              assets={selectedAssets}
               disabled={sending}
-              accessibilityRole="button"
-              accessibilityLabel="Choose photos or videos"
-              style={({ pressed }) => [
-                styles.addButton,
-                pressed && styles.pressed,
-              ]}
-            >
-              <Ionicons name="add" size={25} color={COLORS.text} />
-            </Pressable>
+              onRemove={(assetId) => setSelectedAssets((current) =>
+                current.filter((asset) => asset.id !== assetId)
+              )}
+            />
 
-            <View style={styles.inputWrap}>
-              <TextInput
-                value={input}
-                onChangeText={setInput}
-                placeholder="Message"
-                placeholderTextColor="#9e9e9e"
-                multiline
-                maxLength={4000}
-                editable={!sending}
-                style={styles.input}
-                onFocus={() => setTimeout(() => {
-                  listRef.current?.scrollToOffset?.({
-                    offset: 0,
-                    animated: true,
-                  });
-                }, 60)}
-              />
-            </View>
+            {uploadStage ? (
+              <View style={styles.uploadStage}>
+                <ActivityIndicator size="small" />
+                <Text style={styles.uploadStageText}>{uploadStage}</Text>
+              </View>
+            ) : null}
 
-            <Pressable
-              onPress={send}
-              disabled={
-                sending
-                || (!input.trim() && selectedAssets.length === 0)
-              }
-              accessibilityRole="button"
-              accessibilityLabel="Send message"
-              style={({ pressed }) => [
-                styles.sendButton,
-                input.trim() || selectedAssets.length
-                  ? styles.sendButtonReady
-                  : styles.sendButtonDisabled,
-                pressed && styles.pressed,
-              ]}
-            >
-              {sending ? (
-                <ActivityIndicator color="#fff" size="small" />
-              ) : (
-                <Ionicons name="arrow-up" size={18} color="#fff" />
-              )}
-            </Pressable>
-          </View>
-        </KeyboardAvoidingView>
-      ) : null}
-    </SafeAreaView>
+            <View style={[
+              styles.composer,
+              { paddingBottom: Math.max(8, insets.bottom) },
+            ]}>
+              <Pressable
+                onPress={pickMedia}
+                disabled={sending}
+                accessibilityRole="button"
+                accessibilityLabel="Choose photos or videos"
+                style={({ pressed }) => [
+                  styles.addButton,
+                  pressed && styles.pressed,
+                ]}
+              >
+                <Ionicons name="add" size={25} color={COLORS.text} />
+              </Pressable>
+
+              <View style={styles.inputWrap}>
+                <TextInput
+                  value={input}
+                  onChangeText={setInput}
+                  placeholder="Message"
+                  placeholderTextColor="#9e9e9e"
+                  multiline
+                  maxLength={4000}
+                  editable={!sending}
+                  style={styles.input}
+                  onFocus={() => setTimeout(() => {
+                    listRef.current?.scrollToOffset?.({
+                      offset: 0,
+                      animated: true,
+                    });
+                  }, 60)}
+                />
+              </View>
+
+              <Pressable
+                onPress={send}
+                disabled={
+                  sending
+                  || (!input.trim() && selectedAssets.length === 0)
+                }
+                accessibilityRole="button"
+                accessibilityLabel="Send message"
+                style={({ pressed }) => [
+                  styles.sendButton,
+                  input.trim() || selectedAssets.length
+                    ? styles.sendButtonReady
+                    : styles.sendButtonDisabled,
+                  pressed && styles.pressed,
+                ]}
+              >
+                {sending ? (
+                  <ActivityIndicator color="#fff" size="small" />
+                ) : (
+                  <Ionicons name="arrow-up" size={18} color="#fff" />
+                )}
+              </Pressable>
+            </View>
+          </KeyboardAvoidingView>
+        ) : null}
+      </View>
+    </View>
   );
 }
 
@@ -606,6 +613,38 @@
     flex: 1,
     backgroundColor: COLORS.bg,
   },
+  chatHeader: {
+    flexShrink: 0,
+    backgroundColor: COLORS.bg,
+    borderBottomWidth: StyleSheet.hairlineWidth,
+    borderBottomColor: COLORS.border,
+  },
+  chatHeaderRow: {
+    minHeight: 64,
+    flexDirection: 'row',
+    alignItems: 'center',
+    paddingHorizontal: 4,
+  },
+  chatHeaderIdentity: {
+    flex: 1,
+    minHeight: 58,
+    alignItems: 'center',
+    justifyContent: 'center',
+  },
+  headerSide: {
+    width: 54,
+    minHeight: 54,
+    alignItems: 'center',
+    justifyContent: 'center',
+  },
+  backButton: {
+    alignItems: 'flex-start',
+    paddingLeft: 4,
+  },
+  chatBody: {
+    flex: 1,
+    minHeight: 0,
+  },
   centerState: {
     flex: 1,
     alignItems: 'center',
--- /mnt/data/circles_step9b1_work/src/components/conversations/ConversationHeaderTitle.js	2026-07-21 19:25:18.159205246 +0000
+++ /mnt/data/circles_step9b2_work/src/components/conversations/ConversationHeaderTitle.js	2026-07-21 20:05:27.441898381 +0000
@@ -42,15 +42,18 @@
   root: {
     minWidth: 130,
     maxWidth: 230,
+    minHeight: 54,
     alignItems: 'center',
     justifyContent: 'center',
     paddingHorizontal: 8,
-    paddingVertical: 2,
+    paddingVertical: 4,
   },
   nameRow: {
     maxWidth: '100%',
+    minHeight: 15,
     flexDirection: 'row',
     alignItems: 'center',
+    justifyContent: 'center',
     marginTop: 2,
   },
   name: {
@@ -58,6 +61,7 @@
     color: COLORS.text,
     fontFamily: 'Manrope_600SemiBold',
     fontSize: 11,
+    lineHeight: 14,
   },
   pressed: {
     opacity: 0.62,
